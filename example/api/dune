(library
 (name api_native)
 (wrapped false)
 (libraries lwt dream remote_native realm_native)
 (flags :standard -open Ppx_yojson_conv_lib.Yojson_conv.Primitives -open Realm)
 (preprocess
  (per_module
   ((pps remote_ppx ppx_yojson_conv)
    api_spec)))
 (modes native))

(subdir
 browser
 (library
  (name api_browser)
  (wrapped false)
  (modes melange)
  (flags
   :standard
   -open
   Ppx_yojson_conv_lib.Yojson_conv.Primitives
   -open
   Realm)
  (libraries remote_browser realm_browser)
  (preprocess
   (pps ppx_yojson_conv remote_ppx -js)))
 (rule
  (target api.ml)
  (deps ../api_spec.ml)
  (action
   (copy# ../api_spec.ml %{target}))))
