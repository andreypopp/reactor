(library
 (name routing_native)
 (wrapped false)
 (preprocess
  (pps ppx_deriving_router))
 (modes native))

(subdir
 browser
 (library
  (name routing_browser)
  (wrapped false)
  (modes melange)
  (preprocess
   (pps ppx_deriving_router.browser)))
 (copy_files#
  (files ../**[!.pp][!.mlx].ml)))
